mixin search(keyword)
    form(form-search="")
        .input-group 
            input(
                type="text"
                placeholder="Type here..."
                name="keyword"
                class="form-control"
                value=keyword
            )
            .input.group-append
                button(
                  class="btn btn-success"
                  type="submit"
                ) Search
    
            